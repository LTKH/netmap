[global]
urls = ["http://localhost:8082"]
max_resp_time = "5s"
interval = "60s"

[cache]
limit = 1000
flush_interval = "5m"

[netstat]
enabled = true
send = true
status = ""
ignore_ports = [22,1521]
timeout = "10s"
interval = "900s"
command = "traceroute -m 3 -p {{ .port }} {{ .dst_name }}"

[[connections]]
urls = ["http://localhost:8082"]
command = "traceroute -m 3 -p {{ .port }} {{ .dst_name }}"
interval = "60s"